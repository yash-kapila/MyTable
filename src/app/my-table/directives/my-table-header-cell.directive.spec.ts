import { MyTableHeaderCellDirective } from './my-table-header-cell.directive';

describe('MyTableHeaderCellDirective', () => {
  it('should create an instance', () => {
    const directive = new MyTableHeaderCellDirective();
    expect(directive).toBeTruthy();
  });
});
