import { MyTableDefaultPipePipe } from './my-table-default-pipe.pipe';

describe('MyTableDefaultPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyTableDefaultPipePipe();
    expect(pipe).toBeTruthy();
  });
});
